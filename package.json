{
  "name": "fetch-run",
  "version": "0.1.1",
  "description": "Fetch middlewares for API endpoints",
  "scripts": {
    "format:watch": "onchange 'src/**/*.js' -- prettier --write {{changed}}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "fetch"
  ],
  "author": "Benoit Sagols",
  "license": "ISC",
  "devDependencies": {
    "onchange": "^5.1.0",
    "prettier": "^1.16.2"
  },
  "dependencies": {
    "npm": "^6.4.1"
  }
}
